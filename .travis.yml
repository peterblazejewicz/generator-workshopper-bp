language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-workshopper-bp' ]; then cd .. && eval "mv $currentfolder generator-workshopper-bp" && cd generator-workshopper-bp; fi
